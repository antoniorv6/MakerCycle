import Dashboard from '@/components/Dashboard';

export default function TeamsPage() {
  // Pass a prop or use a query param/context to select the 'teams' page if needed
  return <Dashboard initialPage="teams" />;
} 